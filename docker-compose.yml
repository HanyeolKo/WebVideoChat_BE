services:
  app:
    # GHCR에 push된 이미지를 pull하여 사용한다(서버는 빌드하지 않음).
    image: ghcr.io/hanyeolko/webvideochat-be:latest
    container_name: webvideochat-app
    restart: always
    env_file:
      - .env
    expose:
      - "8080"
    networks:
      - webnet

  nginx:
    image: nginx:1.27-alpine
    container_name: webvideochat-nginx
    restart: always
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/certs:/etc/nginx/certs:ro
    depends_on:
      - app
    networks:
      - webnet

networks:
  webnet:
    driver: bridge
