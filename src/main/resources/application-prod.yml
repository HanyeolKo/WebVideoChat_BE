server:
  port: 8080
  address: 0.0.0.0

# 운영 CORS 허용 오리진. 배포 시 서버 .env의 CORS_ALLOWED_ORIGINS(콤마 구분)로 주입된다.
# 미설정 시 기본값으로 폴백하여 기동 실패를 방지한다.
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173}

logging:
  level:
    root: warn
